{
  "_what": "Ledger of questions that have ALREADY been tested and settled. Each entry names the attack, who made it, the date, the command that settled it, and the result. An incoming critique that matches a settled entry must be answered from this file, NOT by re-running the work.",
  "_rule": "Do not re-run a settled check unless the challenger supplies NEW EVIDENCE that the recorded result is wrong. A restated opinion is not new evidence. Re-running settled work costs the operator money he does not have.",
  "_usage": "python settled.py --check \"<keywords>\"  |  python settled.py --list  |  python settled.py --open",
  "entries": [
    {
      "id": "S01", "settled": "2026-09-03", "status": "SETTLED",
      "attack": "Unseeded backend error rates confound every between-version comparison. FlexibleBackend draws error rates randomly per construction and the two arms build backends separately; at optimization level 2 the layout passes score with error rates.",
      "raised_by": "the author, self-attack",
      "command": "python -c \"see RESEARCH_LANDSCAPE.md sec 46 attack 3\"",
      "result": "0 of 24 (circuit,seed) cases changed the observable, with 4 of 4 VERIFIED-DISTINCT error-rate draws per circuit. The test had power; the confound does not exist.",
      "keywords": ["backend", "error rate", "confound", "unseeded backend", "FlexibleBackend", "optimization level"]
    },
    {
      "id": "S02", "settled": "2026-09-03", "status": "SETTLED",
      "attack": "Consecutive seeds 1000-1011 are not independent draws.",
      "raised_by": "the author, self-attack",
      "command": "RESEARCH_LANDSCAPE.md sec 46 attack 8",
      "result": "lag-1 autocorrelation between -0.22 and +0.11 across four circuits; a scattered seed set spanning 7..2^31 gives matching means and spreads.",
      "keywords": ["consecutive seeds", "seed independence", "autocorrelation", "seed spacing"]
    },
    {
      "id": "S03", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The 200 seeds came from one contiguous block inside one OS process, so a PRNG or process-state artifact could produce the result.",
      "raised_by": "Reviewer A and Reviewer B, round 2",
      "command": "python scatter.py --circuit bv_n140 --topology heavy-hex --n 200 --chunks 10 ...",
      "result": "A DISJOINT set of 200 scattered seeds (5.9M-1.08B) across 10 fresh processes with differing PYTHONHASHSEED gives 22.59% [16.37,30.61] against 26.28% [18.45,34.20]. Zero seed overlap. Each point lies inside the other's interval. Confound removed.",
      "keywords": ["PRNG", "process state", "contiguous", "scattered seeds", "PYTHONHASHSEED", "hash seed", "process diversity"]
    },
    {
      "id": "S04", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The <=10s runtime selection rule biases the sample toward high-relative-variance circuits.",
      "raised_by": "Reviewer B, round 3",
      "command": "python audit.py  (check A7)",
      "result": "REFUTED. Selected median seed spread 0.1126 vs excluded 0.1234, Mann-Whitney p=0.768. If anything the EXCLUDED circuits are more variable. NOTE: this is 'no detected difference', not proof of independence.",
      "keywords": ["selection bias", "10s", "runtime selection", "convenience sample", "Mann-Whitney", "representativeness"]
    },
    {
      "id": "S05", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The integer decision rule q*Sb >= (q+p)*Sa may not equal the intended threshold comparison; check equality, rounding, direction.",
      "raised_by": "the author, self-attack (found a REAL float defect first)",
      "command": "python audit.py  (checks A8, P8.1)",
      "result": "The float form b >= a(1+t) disagreed with the literal rule in 84 of 900 cases, so it was REPLACED by exact integer arithmetic, verified against exact-integer brute force: 900 cases, 0 disagreements. Threshold equality correctly counts as a call.",
      "keywords": ["integer rule", "decision rule", "exactness", "floating point", "equality", "threshold comparison", "rounding"]
    },
    {
      "id": "S06", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "Enumeration draws k seeds WITH replacement, but a maintainer runs k DISTINCT seeds.",
      "raised_by": "the author, self-attack",
      "command": "python audit.py  (check P9)",
      "result": "Both computed on the pre-registered data. Max per-circuit risk difference 0.0011; the count of circuits at risk >=5% is UNCHANGED at 7. Not outcome-determining.",
      "keywords": ["replacement", "with replacement", "without replacement", "enumeration", "combinations", "ordered tuples"]
    },
    {
      "id": "S07", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The ambiguity band uses a multiplicative residual model, but additive fits better on this topology.",
      "raised_by": "the author, self-attack",
      "command": "python model_check.py --topology heavy-hex --pair 200:202 --band-compare",
      "result": "Recomputed additively at 200 seeds: median band 10.86 pp -> 10.73 pp, wider under additive on only 1 of 23 circuits. Robust to the model choice. The assumption must be DISCLOSED but does not change the number.",
      "keywords": ["multiplicative", "additive", "residual model", "ambiguity band model", "band model"]
    },
    {
      "id": "S08", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The theta bootstrap resamples the SAME seed indices across both arms, imposing a covariance the sampling design does not have. Flagged as a possible real inferential error.",
      "raised_by": "Reviewer A, round 3, attack 5",
      "command": "compare joint vs independent arm resampling on the pre-registered data",
      "result": "FAILS. Both arms were measured at the IDENTICAL 200 seeds, so the seed is the sampling unit and paired resampling is correct. Cross-arm correlation median -0.009, 0 of 26 circuits above |0.3|. Joint vs independent intervals differ by <=0.05 pp and produce ZERO verdict flips on 5 tested circuits.",
      "keywords": ["bootstrap", "paired arms", "joint resampling", "independent resampling", "covariance", "arm dependence", "pseudoreplication"]
    },
    {
      "id": "S09", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "Duplicate circuits inflate the endpoint count.",
      "raised_by": "the author, self-attack",
      "command": "python audit.py  (check A3)",
      "result": "swap_test_n41 and knn_n41 are byte-identical seed-for-seed in both arms. NEITHER is in the eligible set (both are BOUNDARY), so the endpoint is unaffected. No other duplicate pair exists.",
      "keywords": ["duplicate", "identical circuits", "knn_n41", "swap_test_n41", "pseudoreplication"]
    },
    {
      "id": "S10", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "Wilson intervals assume the 26 circuit classifications are independent; algorithm-family clustering makes them too narrow.",
      "raised_by": "Reviewer A, round 2",
      "command": "python followup.py  (F8)",
      "result": "CONFIRMED and ACTED ON. Families are perfectly separated (all-hit or all-miss). Cluster bootstrap over families, B=20000: [17.4%, 81.0%] against Wilson [28.8%, 64.5%]. The suite-level proportion was WITHDRAWN as a headline as a result.",
      "keywords": ["Wilson", "cluster", "family", "clustering", "independence", "effective sample size", "12/26", "46.2%"]
    },
    {
      "id": "S11", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The rho = -0.833 correlation is presented as a discovered mechanism.",
      "raised_by": "the author, self-attack during the 16-phase red team",
      "command": "simulate circuits with theta and spread drawn INDEPENDENTLY, no compiler involved",
      "result": "The claim was WITHDRAWN. Synthetic data containing no compiler reproduces the statistic at mean -0.661 over 30 trials of 23 circuits, with 5 of 30 reaching -0.833 or stronger. The association is induced by the decision rule. Already corrected in the paper at commit 52f24dd.",
      "keywords": ["rho", "-0.833", "Spearman", "mechanism", "correlation", "tautology", "mechanistic"]
    },
    {
      "id": "S12", "settled": "2026-09-03", "status": "SETTLED",
      "attack": "The observable or the backend differs across Qiskit versions, confounding the comparison.",
      "raised_by": "the author, self-attack",
      "command": "RESEARCH_LANDSCAPE.md sec 46 attacks 1, 2, 7",
      "result": "two_q_gate_type is 'cz' in all three Qiskit versions AND on all four topologies; transpiled output contains no other 2-qubit gate; coupling map identical across versions (278 edges, hash ef616023); basis gates identical.",
      "keywords": ["observable", "cz", "coupling map", "basis gates", "backend identity", "version comparability"]
    },
    {
      "id": "S13", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The pre-registration was not followed; analysis choices were made after seeing results.",
      "raised_by": "the author, self-attack (audit phase 2)",
      "command": "python audit.py ; git log --diff-filter=A -- prereg_analysis.py results/raw/prereg",
      "result": "Constants match the code EXACTLY: BOOT_TRUTH=4000, BOOT_ERR=400, BOUNDARY_PP=3.0, rng seed 20260904. Commit order verified: pre-registration 23:51:08, analysis code 23:52:38, data 09:13:07 next morning. One amendment (A1) was recorded, for two harness bugs found BEFORE any result was inspected.",
      "keywords": ["preregistration", "prereg", "post hoc", "researcher degrees of freedom", "timestamps", "commit order"]
    },
    {
      "id": "S14", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "Benchpress performs some aggregation over runs (mean or best-of-k), so comparing means is a strawman.",
      "raised_by": "Reviewer B, round 2",
      "command": "grep -rn 'statistics.mean|np.mean|n_runs|num_runs|repeat|addoption' benchpress/",
      "result": "Benchpress performs NO aggregation anywhere: 0 means, 0 run-count options across the whole repository. It records one value per invocation. Separately, min-of-3 was TESTED: all 8 circuits still err and every rate roughly DOUBLES, so best-of-k makes the problem worse.",
      "keywords": ["aggregation", "mean", "best-of-k", "min-of-3", "minimum", "strawman", "estimator choice"]
    },
    {
      "id": "S15", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The abstract and sec 2.1 claim NO gym passes a compiler seed. BQSKit's gym passes seed=0.",
      "raised_by": "the author, verified by reading IBM's source at bqskit_gym/device_transpile/test_summit.py:179",
      "command": "grep -n 'no gym|eight gym|seed_transpiler' PAPER.md",
      "result": "CONFIRMED TRUE, AND NOW FIXED IN THE PAPER at commit 8e23d0c. The abstract states the Qiskit gym compiles unseeded 'while the BQSKit gym in the same repository does seed its compiler', and sec 2.1 carries an explicit scope note that seed_transpiler is a Qiskit-only API the other six gyms cannot call. Verified 2026-09-04: the universal claim no longer appears anywhere in PAPER.md. This entry read OPEN with 'the paper at 52f24dd still contains the false claim' until 2026-09-04; that was stale, and the staleness was itself caught during the F1-F6 validation pass.",
      "keywords": ["eight gyms", "BQSKit", "seed=0", "no gym passes", "seed_transpiler", "source-level claim"]
    },
    {
      "id": "S16", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "The conclusion says adding runs reduces risk 'at quadratic cost', a scaling law the paper never fitted.",
      "raised_by": "the author, self-attack",
      "command": "grep -c quadratic PAPER.md",
      "result": "CONFIRMED, AND NOW FIXED IN THE PAPER at commit 8e23d0c. Our own six points give slope -0.701, not the -0.500 quadratic cost requires. Verified 2026-09-04: the word 'quadratic' occurs ZERO times in PAPER.md. This entry read OPEN with 'the paper at 52f24dd still contains the word' until 2026-09-04; that was stale.",
      "keywords": ["quadratic", "scaling law", "cost model", "k sweep", "runs per version"]
    },
    {
      "id": "S17", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F1. deep.exact_rate_int documents k in {1,2,3} but its else-branch answers with the k=1 arrays for ANY other k, returning a confidently wrong number instead of refusing.",
      "raised_by": "Reviewer C, code-implementation audit",
      "command": "compare exact_rate_int(o=[1,10], n=[2,10], t=0.5, k) against itertools.product brute force",
      "result": "CONFIRMED. k=4 returned 0.5000000 against a true 0.2578125; k=5 returned 0.5000000 against 0.2265625; k=0 and k=-3 also returned the k=1 answer silently. NO PUBLISHED NUMBER AFFECTED: the only caller, prereg_analysis.rate(), routes k>3 to mc_rate, and every reported k (1,3,5,8,10,20) took the correct path. FIXED: unsupported k now raises ValueError.",
      "keywords": ["exact_rate_int", "k branch", "unsupported k", "silent wrong answer", "F1", "else branch"]
    },
    {
      "id": "S18", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F2. prereg_analysis.load() discards seed IDs and analyse() pairs the two arms BY POSITION, so mismatched seed sets would be silently mispaired under sec 5.1's joint resampling.",
      "raised_by": "Reviewer C, code-implementation audit",
      "command": "compare the seed lists of every committed prereg and deep arm pair",
      "result": "CONFIRMED as an unenforced invariant, with ZERO instances in the data: 39 of 39 pre-registered pairs carry identical seed sets, 0 duplicate seeds, and all five deep pairs match 200/200. Positional pairing therefore equalled seed pairing everywhere. NO PUBLISHED NUMBER AFFECTED. FIXED: load_seeded() keeps the seed IDs and analyse() reports status MISALIGNED_SEEDS per sec 7.1 rather than assuming alignment. The regenerated prereg CSV is byte-for-byte unchanged.",
      "keywords": ["seed alignment", "positional pairing", "load", "analyse", "misaligned", "F2", "joint resampling"]
    },
    {
      "id": "S19", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F3. A zero baseline is handled inconsistently: intervals.exact_call_rate([0],[0],0.10,1) returns 1.0 while paired.exact_paired_rate raises ZeroDivisionError.",
      "raised_by": "Reviewer C, code-implementation audit",
      "command": "call both exact estimators on a zero baseline; then census min(two_q) over all raw run rows",
      "result": "CONFIRMED. exact_call_rate returned 1.0 because the integral guard's `o > 0` clause failed, dropping it to the float path where 0 >= 0 counts as a call. UNREACHABLE FROM THE DATA: across all 41,790 recorded run rows the minimum two-qubit gate count is 34 and no row is 0. NO PUBLISHED NUMBER AFFECTED. FIXED: both estimators now raise identically on a non-positive baseline.",
      "keywords": ["zero baseline", "ZeroDivisionError", "exact_call_rate", "exact_paired_rate", "undefined relative change", "F3"]
    },
    {
      "id": "S20", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F4. deep.mc_rate's docstring claims chunking leaves the result 'identical'. Chunking changes the RNG draw order, so it changes the value.",
      "raised_by": "Reviewer C, code-implementation audit",
      "command": "run mc_rate twice at the same seed with MC_CHUNK_ELEMS forced to 10**9 and to 4",
      "result": "CONFIRMED, and an earlier self-test of this finding was INVALID because both of its paths ran in a single chunk. Redone properly: o=[10,11,12,13], n=[11,12,13,14], k=2, 12 pairs, same seed gives 0.500000 in one chunk and 0.333333 in six. Both are unbiased estimates of the same quantity, so NO PUBLISHED NUMBER AFFECTED; what is false is bitwise reproducibility against an unchunked run. FIXED: docstring corrected. Chunking is genuinely exercised (the k=20 fallback splits 4,000,000 pairs into 2 chunks).",
      "keywords": ["mc_rate", "chunking", "draw order", "identical", "reproducibility", "F4", "MC_CHUNK_ELEMS"]
    },
    {
      "id": "S21", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F5. inventory.py --check cannot fail. Each `value` was a function call evaluated when the registry was built and `recompute` re-ran the same call on the same file, so the check compared f() with f(); 31 of 41 entries also re-read the very summary CSV they certify.",
      "raised_by": "Reviewer C, code-implementation audit; quantified and sharpened by the author",
      "command": "falsify results/summary/band_heavy-hex.csv (x3, +40 pp) in a copy, redirect inventory.summary() at it, run --check",
      "result": "CONFIRMED, AND THE MOST SERIOUS OF THE SIX. The falsified data still produced '41/41 live numbers reproduce, 0 failures', printing 82.13 pp where the true median band is 14.04 pp. PAPER.md separately claimed three times that the inventory 're-derives every published figure from raw data', which was FALSE. NO MEASURED NUMBER WAS WRONG: deep_143_200.csv, band_heavy-hex.csv and prereg_heavy-hex.csv were each rebuilt from raw and are byte-for-byte identical, and the pre-registered endpoint reproduced at 12/26 = 0.4615, Wilson [0.2876, 0.6454]. FIXED: every value is now a frozen literal; 29 of 41 rows re-derive from results/raw/*.jsonl and the 12 k-sweep rows are labelled DERIVED with the reason; the three PAPER.md sentences now state exactly this. Re-falsifying the derived CSV now produces 12 FAILURES, and tampering with a recorded value produces 1.",
      "keywords": ["inventory", "circular", "tautology", "cannot fail", "checker", "F5", "re-derives from raw", "verification apparatus", "frozen literal"]
    },
    {
      "id": "S22", "settled": "2026-09-04", "status": "SETTLED",
      "attack": "F6. scatter.py does not emit per-seed failure rows; a failed chunk aborts the parent instead of merging partial rows plus a failure record.",
      "raised_by": "Reviewer C, code-implementation audit",
      "command": "read scatter.py:114-137; census the record types in results/raw/scatter_*.jsonl",
      "result": "CONFIRMED AS DESCRIBED, AND DELIBERATELY NOT FIXED. Both committed scatter files hold exactly 10 env + 200 run records, so nothing was dropped. A failed chunk calls sys.exit and writes NO output file at all, which cannot produce a silently truncated dataset; emitting partial rows plus an error record would CREATE that risk. Fail-loud-with-no-output is the safer design and is kept. NO PUBLISHED NUMBER AFFECTED; S03 already settles the comparison this run supports.",
      "keywords": ["scatter", "failure rows", "chunk abort", "run_error", "partial output", "F6", "fail loud"]
    },
    {
      "id": "S23", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "The paper contradicts itself about Benchpress seeding: the contribution paragraph said the suite 'sets no transpiler seed anywhere' while sec 2.1 correctly records that the BQSKit gym passes seed=0.",
      "raised_by": "GPT-5.6 Sol, external audit; a further instance found by Claude during the repair",
      "command": "grep -nE 'no .{0,15}seed|sets no|anywhere|seven further' PAPER.md",
      "result": "CONFIRMED, and there were TWO bad statements, not one. PAPER.md:67 made the global claim, and PAPER.md:376 separately said 'seven further gyms also pass no seed' -- which would include BQSKit and also contradicts sec 2.1's own statement that the remaining SDKs are NOT assessed. FIXED: the contribution now claims only the Qiskit gym at the pinned revision, and the limitation now states the true arithmetic -- eight gyms, two inspected (Qiskit unseeded, BQSKit seeds), six not assessed. NOTE: the 2026-09-04 fix of this same claim (S15) repaired the abstract and sec 2.1 and MISSED both of these, because it searched for specific phrasings rather than for the claim.",
      "keywords": ["benchpress seeding", "no seed anywhere", "BQSKit", "seven gyms", "eight gyms", "contribution overclaim", "scope"]
    },
    {
      "id": "S24", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "Methods and sec 3.4 claimed fixed-seed determinism was verified 'across processes and machines', while sec 5.2 lists 'one machine' as a limitation.",
      "raised_by": "GPT-5.6 Sol, external audit",
      "command": "python -c \"count distinct platform/cpu_count/python across every env record in results/raw and replication\"",
      "result": "CONFIRMED and the provenance was checked before changing a word. Across all 196 environment records there is exactly ONE distinct platform string (Windows-10-10.0.26200-SP0), ONE cpu_count (16) and ONE python (3.10.10), and the schema records NO cpu vendor or model field at all -- so a second machine is neither documented nor provable from these data. The operator initially recalled an independent AMD/Windows-10 replication, then corrected himself: that rerun was a fresh clone and environment on the SAME physical rig. FIXED: 'machines' removed from both places; the claim now says processes plus an independently reconstructed environment on one machine, and sec 3.4 states explicitly that no cross-hardware claim is supportable. A genuine second-machine run is planned and will be added only with artifact output as evidence.",
      "keywords": ["across machines", "one machine", "determinism", "cross-machine", "platform string", "cpu vendor", "provenance"]
    },
    {
      "id": "S25", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "The introduction said the experiment 'identifies the mechanism that governs it', while sec 4.1 correctly says the distance-risk association is largely induced by the decision rule and is NOT a discovered mechanism.",
      "raised_by": "GPT-5.6 Sol, external audit",
      "command": "grep -n mechanism PAPER.md",
      "result": "CONFIRMED. PAPER.md:72 made the claim and the sec 4.1 HEADING itself read 'The mechanism', both contradicting the warning at :237. FIXED: the contribution now says the experiment 'reports what predicts it -- while declining to call that association a compiler mechanism', and the heading is now 'What predicts the risk -- and why it is not a mechanism'. The empirical content is untouched: seed-induced variability, ambiguity bands, deterministic vs stochastic behaviour and finite-sample decision risk all stand. Related to S11, which withdrew rho = +0.876 for the same reason.",
      "keywords": ["mechanism", "overclaim", "introduction", "4.1 heading", "decision rule induced", "rho"]
    },
    {
      "id": "S26", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "The seed-alignment guard added on 2026-09-04 (F2/S18) used m = min(len(a), len(b)) and compared only the first m seeds, so a 200-seed arm paired with a truncated 199-seed arm sharing that prefix PASSED and the good arm was then silently cut to 199.",
      "raised_by": "GPT-5.6 Sol, external audit -- a defect in the fix Claude wrote four days earlier",
      "command": "python -m pytest tests/test_seed_alignment.py -q",
      "result": "CONFIRMED. The guard detected MISPAIRING but not TRUNCATION, so it was weaker than its own comment claimed. FIXED: analyse() now requires equal arm lengths, no duplicate seed ids in either arm, and identical COMPLETE seed arrays; it returns MISALIGNED_SEEDS or DUPLICATE_SEEDS with a machine-readable reason and never truncates. Eight regression tests pin the three refusals and the acceptance: 200-vs-199 prefix, equal-length different identity, duplicate seed, the valid case keeping all 12 seeds, refusals reporting n_seeds 0, and no refused row reaching endpoint(). ACCEPTANCE TEST PASSED: the committed prereg summary regenerates BYTE-FOR-BYTE IDENTICAL, so no published number moved.",
      "keywords": ["seed alignment", "truncation", "min length", "duplicate seeds", "guard", "prereg_analysis", "byte-identical"]
    },
    {
      "id": "S27", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "sec 5.3 said the hostile audit 're-derived every reported number from raw data', which inventory.py's own documentation says would be false.",
      "raised_by": "GPT-5.6 Sol, external audit",
      "command": "grep -nE 're-derived every|every reported number' PAPER.md",
      "result": "CONFIRMED. This was a FOURTH instance of the claim corrected on 2026-09-04 (S21) -- three sentences were fixed then and PAPER.md:392 was missed, again because the fix matched phrasings instead of hunting the claim. FIXED: sec 5.3 now states the actual tiers, 29 of 41 re-derived from raw per-seed measurements and 12 (the sec 43 k-sweep) from a derived summary table, says why the k-sweep is not affordable to re-derive inside a one-minute check, and states plainly that no claim of full raw re-derivation is made. The later code audit and its six findings, S17-S22, are now also disclosed there.",
      "keywords": ["re-derived from raw", "every reported number", "inventory tiers", "29 of 41", "k-sweep", "overclaim"]
    },
    {
      "id": "S28", "settled": "2026-09-08", "status": "SETTLED",
      "attack": "verify.py did not run paper_check.py, so the manuscript could drift from the code without any check failing; and paper_check's core assertion is only that a formatted number appears SOMEWHERE in PAPER.md.",
      "raised_by": "GPT-5.6 Sol, external audit",
      "command": "BENCHPRESS_PATH=... envs/bp202/Scripts/python.exe verify.py",
      "result": "CONFIRMED on both counts. FIXED: paper_check.py is now stage 6 of verify.py (6/6 PASS, 95s total), and check() gained a `near=` anchor binding a value to a location instead of the whole document. The twelve load-bearing endpoint-table figures are now bound to their table ROW. The anchoring immediately earned its keep: anchored to 'risk >= 5%' it FAILED, because that string first occurs in a sec 3.2 prose sentence ninety lines above the table -- exactly the wrong-location failure it exists to catch. Anchors now include the leading pipe. ⚠ STATED, NOT HIDDEN: this is a narrowing, not a cure. Presence near an anchor still does not prove a number is quoted in the right claim, and verify.py's docstring says so.",
      "keywords": ["verify.py", "paper_check", "substring", "presence not placement", "anchor", "near", "stage 6"]
    },
    {
      "id": "S29", "settled": "2026-09-09", "status": "SETTLED",
      "attack": "Determinism was claimed 'across machines' with no second machine in the record (S24), so the claim was withdrawn. Does a fixed seed actually reproduce on different hardware, or was the withdrawal hiding a real machine dependence?",
      "raised_by": "GPT-5.6 Sol raised the contradiction; the operator supplied a second physical machine to settle it",
      "command": "crossmachine/measure.py on each machine, then crossmachine/compare.py for all three Qiskit versions; re-verified from the primary machine after merging",
      "result": "EARNED, NOT ASSUMED. All 216 per-seed gate counts (6 circuits x 12 seeds x 3 versions) are IDENTICAL across an AMD64 AuthenticAMD 16-core box and an Intel64 GenuineIntel 8-core box, INCLUDING the 1.4.3 -> 2.0.0 pair the primary study uses. The frozen artifact also passed unmodified on the second machine: 144/144 per-seed values, both negative controls constant, all six ambiguity bands matching to 0.00 pp. Python differed uncontrolled (3.10.10 vs 3.10.21) and the integers still matched. Pre-registered before the second machine existed and inherits the frozen six-circuit twelve-seed selection, so there was no selection freedom. §3.4 now states the two-machine claim; §5.2 still records that the 200-seed study itself ran on ONE machine and only the determinism control is cross-machine. ⚠ SCOPE, STATED IN THE PAPER: both machines ran rustworkx 0.18.1, so this CANNOT separate 'the CPU does not matter' from 'the graph library was identical, so the CPU never had the opportunity to matter'. A differing rustworkx build is the untested case.",
      "keywords": ["cross-machine", "two machines", "determinism", "AMD", "Intel", "rustworkx", "216", "second machine", "fixed seed", "reproducibility"]
    },
    {
      "id": "S30", "settled": "2026-09-09", "status": "SETTLED",
      "attack": "Two defects in the cross-machine tooling itself, found by the second machine while running it: measure.py did not record the rustworkx version, and compare.py's same-machine test ignored the CPU vendor it was already recording.",
      "raised_by": "the second machine, reviewing the tooling it had just been handed",
      "command": "grep -c rustworkx crossmachine/desktop_q143.jsonl ; read compare.py same_box",
      "result": "BOTH CONFIRMED AND FIXED. (1) PREREGISTRATION.md names a differing rustworkx build as 'the interesting one' -- the component most likely to make routing architecture-dependent -- and measure.py then failed to record it, so the first run cannot distinguish the weak reading from the strong one. rustworkx is now in the env record; the desktop reference files were deliberately NOT regenerated, because a reference must not be rewritten after the test has run against it, and the desktop version (0.18.1, all three envs) is recorded here instead. (2) compare.py tested platform == platform and cpu_count == cpu_count while ignoring `processor`. It resolved correctly on 2026-09-09 only because the core counts differed, 16 vs 8; the platform strings were byte-identical. Two boxes with the same OS build and the same core count would have been flagged as possibly-one-machine even across vendors. `processor` is now part of the test.",
      "keywords": ["rustworkx not recorded", "same_box", "processor", "cpu vendor", "measure.py", "compare.py", "tooling defect"]
    },
    {
      "id": "S31", "settled": "2026-09-09", "status": "SETTLED",
      "attack": "Four inputs that crossmachine/compare.py certified as a successful cross-machine replication and should have refused: (1) a file compared against a COPY OF ITSELF with `processor` or `cpu_count` deleted was announced as 'Recorded on two distinct machines'; (2) a duplicate (circuit, seed) row silently overwrote an earlier contradicting one, and int() truncated a candidate 60.9 into agreement with a reference 60, so 'a single differing integer is a finding' was not enforced; (3) a candidate declaring topology='linear', optimization_level=0, a zeroed source-QASM hash, or a second env record naming a different Qiskit version was accepted and compared; (4) two files containing no measurements at all printed 'ALL 0 per-seed gate counts are IDENTICAL'.",
      "raised_by": "ChatGPT Astra 6, external implementation audit, 4 MAJOR / 0 CRITICAL",
      "command": "pytest tests/test_crossmachine_compare.py -q ; python crossmachine/compare.py --reference crossmachine/desktop_q{143,200,202}.jsonl --candidate crossmachine/laptop_q{143,200,202}.jsonl --require-distinct-machines",
      "result": "ALL FOUR CONFIRMED BY EXECUTION AND FIXED. NO PUBLISHED NUMBER MOVED. Every attack was reproduced against the unmodified script before anything was changed, and the committed cross-machine data was then audited against each failure mode WITHOUT USING compare.py: all six files carry exactly one env record, 72 runs, zero duplicate keys, zero non-integer counts, exact coverage of the frozen 6x12 selection, and agreeing topology / optimisation level / seed list / Benchpress commit / module hashes / source-QASM hashes; the AMD and Intel processor strings differ as claimed. Direct comparison is still 216/216 identical, so S29 and PAPER.md §3.4 are untouched. What was broken was the checker's ability to fail, not the evidence. Fixes: `unknown` and `same_box` are now both derived from one MACHINE tuple (platform, processor, cpu_count), because patching a field at a time is exactly what produced this defect twice -- S30 was the first occurrence and this was the same bug surviving on the two fields S30 did not touch; the loader coerces nothing and overwrites nothing, refusing duplicate keys and non-integer counts; provenance must agree across the two files before any count is compared; the expected key set is IMPORTED from replication/replicate.py so PREREGISTRATION.md's no-selection-freedom claim stays true, and anything short of it exits INCOMPLETE. Exit codes are now distinct (0 pass, 1 counts differ, 2 incomplete, 3 refused, 4 machines not distinct) and a new --require-distinct-machines flag turns the machine verdict from printed text into an exit code, because a tool that warns and exits 0 is one an automated caller cannot distinguish from a pass -- which is how occurrence one survived. Attacking the repair before handing it back to the auditor found two more inputs of the same class, both now refused: two files that counted DIFFERENT GATES were comparable (`two_q` is a count of `two_q_gate`, so equal integers mean nothing if the gate differs), and a dirty Benchpress checkout passed because `benchpress_module_sha256` covers only the five pinned modules, so an edit anywhere else in Benchpress was invisible. Both are benign in the committed data -- all six files report `cz` and `benchpress_dirty: false`. 23 regression tests in tests/test_crossmachine_compare.py pin all six attacks plus the negative cases. verify.py 6/6.",
      "keywords": ["compare.py", "absence as difference", "unknown not different", "duplicate measurement", "int truncation", "provenance mismatch", "empty run passes", "frozen selection", "require-distinct-machines", "exit code", "Astra", "checker cannot fail"]
    }
  ]
}
